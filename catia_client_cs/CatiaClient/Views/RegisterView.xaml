<UserControl x:Class="CatiaClient.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="350">
            <TextBlock Text="FSAE-PLM 注册" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>
            <TextBlock Text="用户名" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>
            <TextBlock Text="密码" Margin="0,0,0,5"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,15"/>
            <TextBlock Text="姓名 *" Margin="0,0,0,5"/>
            <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>
            <TextBlock Text="部门" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Department, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>
            <TextBlock Text="加入年份" Margin="0,0,0,5"/>
            <TextBox Text="{Binding JoinYear, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>
            <TextBlock Text="联系电话" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                <Button Content="注册" Command="{Binding RegisterCommand}" Width="100" Margin="0,0,10,0"/>
                <Button Content="返回登录" Command="{Binding BackCommand}" Width="100"/>
            </StackPanel>
            <TextBlock Text="{Binding SuccessMessage}" Foreground="Green" Margin="0,5,0,0" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,5,0,0" TextWrapping="Wrap"/>
            <ProgressBar IsIndeterminate="True" Height="4" Margin="0,10,0,0" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"/>
        </StackPanel>
    </Grid>
</UserControl>
